# __NAME__

MemoMind Web plugin (`__ID__`).

```sh
./mm studio __NAME__     # run in PhoneSDK Browser Studio
./mm build __NAME__      # package to build/web/__NAME__.mmpkg
```

`vendor/gm-plugin-web-sdk.esm.js` is generated by `./mm build` and is not
committed. Declare every Bridge method you call in `manifest.json`.
